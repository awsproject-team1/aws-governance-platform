# Governance Domain Runtime 의존성.
#
# `apps/backend/requirements.txt`와 같은 방식으로 배포 단위별로 분리한다. pypdf는
# `sources/loaders/pdf.py`가 고객 업로드 PDF의 실제 객체와 Object Stream을 여는 데
# 사용하는 Runtime 의존성이며 개발 도구가 아니다. 신뢰할 수 없는 입력을 파싱하므로
# version을 정확히 고정하고 상승 시 함께 검토한다.
pypdf==6.16.1
