# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# Node / frontend
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Terraform local state and working data
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfplan

# Local environment and secrets
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local test/runtime artifacts
.cache/
tmp/
*.log

# 구버전 문서 체계로 작업했던 참고 스냅샷은 리포 밖(../awsproject-before-reference)으로
# 옮겼다. 정본은 docs/ 다. 이 규칙은 스냅샷이 다시 리포 안으로 복사되는 사고를 막는 안전장치다.
before/

# Keep Terraform dependency lock files tracked:
# .terraform.lock.hcl
